apiVersion: v1
kind: Service
metadata:
  name: falkordb
  namespace: agentic-core
  labels:
    app: falkordb
spec:
  type: ClusterIP
  ports:
    - name: falkordb
      port: 6380
      targetPort: 6379
  selector:
    app: falkordb
