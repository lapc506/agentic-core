apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: agentic-core
  labels:
    app: postgres
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  selector:
    app: postgres
